import React, {useEffect, useState} from 'react';
import {Link} from 'react-router-dom';

const Museos = () => {
    return(
        <div>
        <Link to="/museos/crear">Crear Museo</Link>
        </div>
    )
}

export default Museos;